{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "expo run:android",
    "ios": "expo run:ios",
    "start": "react-native start",
    "lint": "eslint .",
    "detox:ios:build": "detox build -c ios",
    "detox:ios:test": "detox test -c ios",
    "detox:ios": "npm run detox:ios:build && npm run detox:ios:test",
    "detox:android:build": "detox build -c android",
    "detox:android:test": "detox test -c android",
    "detox:android": "npm run detox:android:build && npm run detox:android:test"
  },
  "dependencies": {
    "expo": "~54.0.33",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-launch-arguments": "file:./react-native-launch-arguments-4.1.1.tgz"
  },
  "devDependencies": {
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "@types/react": "~19.1.10",
    "babel-preset-expo": "^54.0.10",
    "detox": "^20.33.0",
    "eslint": "^7.32.0",
    "eslint-plugin-detox": "^1.0.0",
    "jest": "~29.7.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
